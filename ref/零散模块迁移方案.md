# 零散模块迁移方案 — 签到/抽奖活动/翻译/客服

## 概述

把PHP后端的4个零散功能模块迁移到NestJS项目。

**项目路径：** `/Users/hulonghua/workspace/kangaroo-japan-backend/`

---

## 模块一：签到系统

### 小程序调用的API

| 原API | 说明 |
|-------|------|
| GET `api/sign/index` | 获取签到数据（积分、已签到天数、优惠券列表） |
| POST `api/sign/sign` | 执行签到 |

### 返回数据结构

`api/sign/index` 返回：
```json
{
  "code": 0,
  "data": {
    "myscore": 150,
    "signDays": [
      {"index": 1, "score": 5, "signed": true},
      {"index": 2, "score": 10, "signed": false},
      {"index": 3, "score": 15, "signed": false},
      {"index": 4, "score": 15, "signed": false},
      {"index": 5, "score": 15, "signed": false},
      {"index": 6, "score": 15, "signed": false},
      {"index": 7, "score": 25, "signed": false}
    ],
    "coupons": []  // 签到可领取的优惠券列表
  }
}
```

### 表结构

用户积分可以直接用 `User` 表的 `score` 字段（已存在）。签到记录需要新表。

```sql
CREATE TABLE sign_logs (
  id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id VARCHAR(36) NOT NULL,
  sign_date DATE NOT NULL,              -- 签到日期
  day_index INT NOT NULL,               -- 第几天（1-7）
  score INT NOT NULL DEFAULT 0,         -- 获得的积分
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_sign_logs_user ON sign_logs(user_id, sign_date);
```

### 代码结构

```
src/sign/
├── sign.module.ts
├── sign.controller.ts
├── sign.service.ts
└── sign-log.entity.ts
```

### API设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/sign/index` | 签到信息（需登录） |
| POST | `/api/v1/sign/sign` | 执行签到（需登录） |

### sign-log.entity.ts

```typescript
import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, Index } from 'typeorm';

@Entity('sign_logs')
@Index('idx_sign_logs_user_date', ['userId', 'signDate'])
export class SignLog {
  @PrimaryGeneratedColumn('uuid')
  id: string;

  @Column({ name: 'user_id' })
  userId: string;

  @Column({ name: 'sign_date', type: 'date' })
  signDate: Date;

  @Column({ name: 'day_index', type: 'int' })
  dayIndex: number;

  @Column({ name: 'score', type: 'int', default: 0 })
  score: number;

  @CreateDateColumn({ name: 'created_at' })
  createdAt: Date;
}
```

### sign.service.ts

```typescript
import { Injectable, BadRequestException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository, DataSource } from 'typeorm';
import { SignLog } from './sign-log.entity';
import { User } from '../users/user.entity';

@Injectable()
export class SignService {
  constructor(
    @InjectRepository(SignLog)
    private signLogRepository: Repository<SignLog>,
    private dataSource: DataSource,
  ) {}

  // 签到积分规则
  private readonly SCORE_RULES = [5, 10, 15, 15, 15, 15, 25];

  async getSignInfo(userId: string) {
    const user = await this.dataSource.getRepository(User).findOne({ where: { id: userId } });
    if (!user) throw new BadRequestException('用户不存在');

    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // 获取本周签到记录（最近7天）
    const weekAgo = new Date(today);
    weekAgo.setDate(weekAgo.getDate() - 7);
    const logs = await this.signLogRepository.find({
      where: { userId, signDate: { $gte: weekAgo } as any },
      order: { signDate: 'ASC' },
    });

    // 构建7天列表
    const signDays = this.SCORE_RULES.map((score, index) => {
      const day = index + 1;
      const log = logs.find(l => l.dayIndex === day);
      return { index: day, score, signed: !!log };
    });

    // 检查今天是否已签到
    const todayLog = await this.signLogRepository.findOne({
      where: { userId, signDate: today },
    });

    return {
      myscore: Number(user.score || 0),
      signDays,
      coupons: [], // 签到优惠券（后续扩展）
      todaySigned: !!todayLog,
      todayScore: todayLog ? todayLog.score : 0,
    };
  }

  async doSign(userId: string) {
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    const existing = await this.signLogRepository.findOne({
      where: { userId, signDate: today },
    });
    if (existing) {
      return { code: 1, errmsg: '您今天已经签过到了' };
    }

    // 计算是连续第几天
    const yesterday = new Date(today);
    yesterday.setDate(yesterday.getDate() - 1);
    const yesterdayLog = await this.signLogRepository.findOne({
      where: { userId, signDate: yesterday },
    });

    let dayIndex = 1;
    if (yesterdayLog) {
      dayIndex = yesterdayLog.dayIndex >= 7 ? 1 : yesterdayLog.dayIndex + 1;
    }
    const score = this.SCORE_RULES[dayIndex - 1];

    await this.dataSource.transaction(async (manager) => {
      const log = this.signLogRepository.create({
        userId,
        signDate: today,
        dayIndex,
        score,
      });
      await manager.save(log);

      await manager
        .createQueryBuilder()
        .update(User)
        .set({ score: () => `score + ${score}` })
        .where('id = :id', { id: userId })
        .execute();
    });

    return { code: 0, errmsg: `签到成功，获得${score}积分` };
  }
}
```

### sign.controller.ts

```typescript
import { Controller, Get, Post, Req, UseGuards, HttpCode, HttpStatus, UnauthorizedException } from '@nestjs/common';
import type { Request } from 'express';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { SignService } from './sign.service';

interface AuthenticatedRequest extends Request {
  user?: { id: string };
}

@Controller('api/v1/sign')
export class SignController {
  constructor(private readonly signService: SignService) {}

  @Get('index')
  @UseGuards(JwtAuthGuard)
  @HttpCode(HttpStatus.OK)
  async index(@Req() req: AuthenticatedRequest) {
    const userId = req.user?.id;
    if (!userId) throw new UnauthorizedException();
    const data = await this.signService.getSignInfo(userId);
    return { code: 0, data };
  }

  @Post('sign')
  @UseGuards(JwtAuthGuard)
  @HttpCode(HttpStatus.OK)
  async sign(@Req() req: AuthenticatedRequest) {
    const userId = req.user?.id;
    if (!userId) throw new UnauthorizedException();
    return await this.signService.doSign(userId);
  }
}
```

### sign.module.ts

```typescript
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { SignLog } from './sign-log.entity';
import { SignController } from './sign.controller';
import { SignService } from './sign.service';
import { User } from '../users/user.entity';

@Module({
  imports: [TypeOrmModule.forFeature([SignLog, User])],
  controllers: [SignController],
  providers: [SignService],
})
export class SignModule {}
```

---

## 模块二：活动提交（抽奖）

### 小程序调用的API

| 原API | 说明 |
|-------|------|
| POST `api/hpsd/submit` | 提交活动（截图+分享链接） |
| POST `api/files/uploadpic` | 上传图片 |

### 表结构

```sql
CREATE TABLE activity_submissions (
  id VARCHAR(36) PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id VARCHAR(36) NOT NULL,
  pictures TEXT,             -- 图片路径，逗号分隔
  remark TEXT,               -- 分享链接
  status VARCHAR(32) DEFAULT 'pending',  -- pending, approved, rejected
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_activity_submissions_user ON activity_submissions(user_id);
```

### 代码结构

```
src/activity/
├── activity.module.ts
├── activity.controller.ts
├── activity.service.ts
└── activity-submission.entity.ts
```

### API设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/activity/submit` | 提交活动（需登录） |
| POST | `/api/v1/files/upload` | 上传图片 |

---

## 模块三：翻译

### 小程序调用的API

| 原API | 说明 |
|-------|------|
| POST `api/trans2zh/jp2zh` | 日文→中文翻译 |

### 实现方式

直接用API代理到翻译服务。简单实现：用百度翻译API或直接调用PHP后端代理。

### API设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/translate/jp2zh` | 日文→中文翻译 |

### 代码结构

```
src/translate/
├── translate.module.ts
├── translate.controller.ts
└── translate.service.ts
```

---

## 模块四：客服

### 小程序调用的API

| 原API | 说明 |
|-------|------|
| POST `api/chat/getkefu` | 获取客服webview链接 |
| POST `api/index/kefu` | 获取客服信息（企业微信二维码等） |

### API设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/chat/kefu` | 获取客服信息 |

### 实现方式

客服信息是静态配置，直接返回配置数据。

### 代码结构

```
src/chat/
├── chat.module.ts
├── chat.controller.ts
└── chat.service.ts
```

---

## 修改app.module.ts

需要添加的实体：
- SignLog
- ActivitySubmission

需要添加的模块：
- SignModule
- ActivityModule
- TranslateModule
- ChatModule
